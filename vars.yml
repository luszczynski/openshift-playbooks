---
subdomain: apps.example.com
domain: example.com
number_of_nodes: 3
remove_images_on_cleanup: false
user: root
pass: redhat@123

# This user needs to be a cluster-admin (oadm policy add-role-to-user cluster-admin superuser && oadm policy add-cluster-role-to-user system:image-pruner superuser)
user_cluster: superuser
pass_cluster: redhat@123

# Registry
secure_registry: false
registry_ha: false
label_registry:

# Router
expose_router_metrics: false
router_ha: false
label_router:

http_proxy:
https_proxy:
no_proxy:
proxy_user:
proxy_pass:
proxy_port:

sdn_subnet: 10.1.0.0/16
service_subnet: 172.30.0.0/16

rhn_user:
rhn_pass:
